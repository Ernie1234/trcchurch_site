export type TPrograms = {
  slug: {
    current: string;
  };
  url: string;
  title: string;
  theme: string;
  guestMinister: string;
  contactInfo: string;
  image: {
    asset: {
      url: string;
    };
  };
  category: string;
  link: string;
  date: string;
  info: string;
};
